# Base URL of the deployed Express backend (API + image/PDF assets).
# Leave UNSET for local development (Vite proxies /api and serves /images, /pdfs).
# Set this in production (e.g. Vercel project env vars) to your backend's URL — no trailing slash.
# VITE_API_BASE_URL=https://your-backend-host.example.com
